# 🤝 Руководство по участию в разработке BaiMuras Platform

Спасибо за ваш интерес к участию в разработке BaiMuras Platform! Этот документ содержит руководящие принципы и информацию для участников проекта.

## 📋 Содержание

1. [Кодекс поведения](#кодекс-поведения)
2. [Начало работы](#начало-работы)
3. [Настройка среды разработки](#настройка-среды-разработки)
4. [Внесение изменений](#внесение-изменений)
5. [Отправка изменений](#отправка-изменений)
6. [Стиль кода](#стиль-кода)
7. [Тестирование](#тестирование)
8. [Документация](#документация)
9. [Сообщество](#сообщество)

## 📜 Кодекс поведения

Этот проект и все участники руководствуются нашим Кодексом поведения. Участвуя в проекте, вы обязуетесь соблюдать этот кодекс.

### Наше обязательство

Мы обязуемся сделать участие в нашем проекте свободным от притеснений для всех, независимо от возраста, телосложения, инвалидности, этнической принадлежности, гендерной идентичности и самовыражения, уровня опыта, национальности, внешнего вида, расы, религии или сексуальной идентичности и ориентации.

### Наши стандарты

Примеры поведения, способствующего созданию позитивной среды:

- Использование дружелюбного и инклюзивного языка
- Уважение к различным точкам зрения и опыту
- Конструктивное принятие критики
- Сосредоточение на том, что лучше для сообщества
- Проявление эмпатии к другим участникам сообщества

## 🚀 Начало работы

### Предварительные требования

Перед началом убедитесь, что у вас установлено:

- Python 3.11 или выше
- Git
- PostgreSQL 13+
- Redis 6+
- Node.js 18+ (для инструментов сборки фронтенда)

### Форк и клонирование

1. Сделайте форк репозитория на GitHub
2. Клонируйте ваш форк локально:
   ```bash
   git clone https://github.com/YOUR_USERNAME/baimuras.space.git
   cd baimuras.space
   ```

3. Добавьте оригинальный репозиторий как upstream:
   ```bash
   git remote add upstream https://github.com/ardakchapaev/baimuras.space.git
   ```

## 🛠️ Настройка среды разработки

### 1. Создание виртуального окружения

```bash
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
pip install -r requirements-dev.txt  # Зависимости для разработки
```

### 3. Настройка окружения

```bash
cp .env.example .env
# Отредактируйте .env с вашими локальными настройками
```

### 4. Настройка базы данных

```bash
# Создание базы данных
createdb baimuras_dev

# Запуск миграций
flask db upgrade
```

### 5. Установка pre-commit хуков

```bash
pre-commit install
```

### 6. Запуск приложения

```bash
python wsgi.py
```

Приложение будет доступно по адресу `http://localhost:5000`.

## 🔄 Внесение изменений

### Соглашения по именованию веток

Используйте описательные имена веток с префиксами:

- `feature/` - для новых функций
- `bugfix/` - для исправления багов
- `hotfix/` - для срочных исправлений
- `docs/` - для изменений в документации
- `refactor/` - для рефакторинга кода
- `test/` - для добавления тестов

Примеры:
- `feature/user-authentication`
- `bugfix/contact-form-validation`
- `docs/api-documentation`

### Формат сообщений коммитов

Следуйте формату conventional commits:

```
type(scope): description

[optional body]

[optional footer]
```

Типы:
- `feat` - новая функция
- `fix` - исправление бага
- `docs` - изменения в документации
- `style` - изменения форматирования
- `refactor` - рефакторинг кода
- `test` - добавление тестов
- `chore` - задачи обслуживания

Примеры:
```
feat(auth): добавить функциональность регистрации пользователей

fix(contact): исправить проблемы валидации формы

docs(api): обновить документацию эндпоинтов
```

### Рабочий процесс разработки

1. **Создайте новую ветку**:
   ```bash
   git checkout -b feature/your-feature-name
   ```

2. **Внесите изменения**:
   - Пишите чистый, читаемый код
   - Следуйте существующему стилю кода
   - Добавляйте тесты для новой функциональности
   - Обновляйте документацию при необходимости

3. **Протестируйте изменения**:
   ```bash
   # Запуск тестов
   pytest

   # Проверка стиля кода
   flake8 src/
   pylint src/

   # Проверка безопасности
   bandit -r src/
   ```

4. **Зафиксируйте изменения**:
   ```bash
   git add .
   git commit -m "feat(scope): ваше описательное сообщение"
   ```

5. **Отправьте в ваш форк**:
   ```bash
   git push origin feature/your-feature-name
   ```

## 📤 Отправка изменений

### Процесс Pull Request

1. **Обновите вашу ветку** последними изменениями:
   ```bash
   git fetch upstream
   git rebase upstream/main
   ```

2. **Создайте Pull Request** на GitHub с:
   - Четким заголовком и описанием
   - Ссылкой на связанные issues
   - Скриншотами для изменений UI
   - Инструкциями по тестированию

3. **Шаблон Pull Request**:
   ```markdown
   ## Описание
   Краткое описание изменений

   ## Тип изменения
   - [ ] Исправление бага
   - [ ] Новая функция
   - [ ] Критическое изменение
   - [ ] Обновление документации

   ## Тестирование
   - [ ] Тесты проходят локально
   - [ ] Добавлены новые тесты
   - [ ] Выполнено ручное тестирование

   ## Скриншоты
   (если применимо)

   ## Связанные Issues
   Closes #123
   ```

### Процесс ревью

- Все PR требуют как минимум одного ревью
- Оперативно отвечайте на отзывы ревьюеров
- Держите PR сфокусированными и разумного размера
- Убедитесь, что проверки CI проходят

## 🎨 Стиль кода

### Стиль Python кода

Мы следуем PEP 8 с некоторыми модификациями:

- Длина строки: 88 символов (форматтер Black)
- Используйте type hints для параметров функций и возвращаемых значений
- Используйте docstrings для всех публичных функций и классов
- Организация импортов: стандартная библиотека, сторонние, локальные импорты

### Форматирование кода

Мы используем автоматические инструменты форматирования:

```bash
# Форматирование кода с Black
black src/

# Сортировка импортов с isort
isort src/

# Проверка с flake8
flake8 src/
```

### Пример стиля кода

```python
from typing import Optional, List
import logging

from flask import Flask, request, jsonify
from werkzeug.exceptions import BadRequest

from src.models import User
from src.utils import validate_email


logger = logging.getLogger(__name__)


class UserService:
    """Сервис для управления операциями с пользователями."""
    
    def __init__(self, db_session):
        self.db_session = db_session
    
    def create_user(self, email: str, name: str) -> Optional[User]:
        """Создать нового пользователя с валидацией.
        
        Args:
            email: Email адрес пользователя
            name: Полное имя пользователя
            
        Returns:
            Созданный экземпляр пользователя или None при неудачной валидации
            
        Raises:
            BadRequest: Если формат email неверный
        """
        if not validate_email(email):
            raise BadRequest("Неверный формат email")
        
        user = User(email=email, name=name)
        self.db_session.add(user)
        self.db_session.commit()
        
        logger.info(f"Создан пользователь: {email}")
        return user
```

### Стиль фронтенд кода

Для JavaScript/CSS:

- Используйте 2 пробела для отступов
- Используйте точки с запятой в JavaScript
- Используйте осмысленные имена переменных
- Комментируйте сложную логику

## 🧪 Тестирование

### Структура тестов

```
tests/
├── unit/           # Модульные тесты
├── integration/    # Интеграционные тесты
├── functional/     # Функциональные тесты
└── fixtures/       # Тестовые данные
```

### Написание тестов

```python
import pytest
from src.models import User
from src.services import UserService


class TestUserService:
    """Тестовые случаи для UserService."""
    
    def test_create_user_success(self, db_session):
        """Тест успешного создания пользователя."""
        service = UserService(db_session)
        user = service.create_user("test@example.com", "Test User")
        
        assert user is not None
        assert user.email == "test@example.com"
        assert user.name == "Test User"
    
    def test_create_user_invalid_email(self, db_session):
        """Тест создания пользователя с неверным email."""
        service = UserService(db_session)
        
        with pytest.raises(BadRequest):
            service.create_user("invalid-email", "Test User")
```

### Запуск тестов

```bash
# Запуск всех тестов
pytest

# Запуск с покрытием
pytest --cov=src

# Запуск конкретного файла тестов
pytest tests/unit/test_user_service.py

# Запуск с подробным выводом
pytest -v
```

## 📚 Документация

### Документация кода

- Используйте docstrings для всех публичных функций, классов и модулей
- Следуйте формату Google docstring
- Включайте type hints
- Документируйте сложные алгоритмы и бизнес-логику

### Документация API

- Обновляйте OpenAPI/Swagger спецификации для изменений API
- Включайте примеры запросов/ответов
- Документируйте коды ошибок и сообщения

### Пользовательская документация

- Обновляйте README.md для изменений в настройке
- Обновляйте документацию по развертыванию
- Добавляйте примеры для новых функций

## 🌟 Сообщество

### Каналы связи

- **GitHub Issues**: Отчеты об ошибках и запросы функций
- **GitHub Discussions**: Общие вопросы и идеи
- **Email**: admin@baimuras.space для приватных вопросов
- **Telegram**: @baimuras_support для быстрой поддержки

### Получение помощи

Если вам нужна помощь:

1. Проверьте существующую документацию
2. Поищите в GitHub issues
3. Задайте вопрос в GitHub Discussions
4. Свяжитесь с мейнтейнерами

### Признание

Участники будут отмечены в:

- Файле CONTRIBUTORS.md
- Заметках к релизам
- Документации проекта

## 🔒 Безопасность

### Сообщение о проблемах безопасности

**Не сообщайте об уязвимостях безопасности через публичные GitHub issues.**

Вместо этого напишите нам на: security@baimuras.space

Включите:
- Описание уязвимости
- Шаги для воспроизведения
- Потенциальное влияние
- Предлагаемое исправление (если есть)

### Руководящие принципы безопасности

- Никогда не коммитьте секреты или учетные данные
- Используйте переменные окружения для конфигурации
- Следуйте руководящим принципам безопасности OWASP
- Валидируйте все пользовательские входные данные
- Используйте параметризованные запросы

## 🚀 Процесс релиза

### Версионирование

Мы используем Semantic Versioning (SemVer):

- MAJOR: Критические изменения
- MINOR: Новые функции (обратно совместимые)
- PATCH: Исправления багов (обратно совместимые)

### Чек-лист релиза

- [ ] Обновить номера версий
- [ ] Обновить CHANGELOG.md
- [ ] Запустить полный набор тестов
- [ ] Обновить документацию
- [ ] Создать заметки к релизу
- [ ] Пометить релиз тегом
- [ ] Развернуть в продакшн

## 🎯 Области для участия

### 🔧 Разработка
- Новые функции
- Исправление багов
- Оптимизация производительности
- Рефакторинг кода

### 🎨 Дизайн
- UI/UX улучшения
- Адаптивный дизайн
- Доступность
- Анимации и переходы

### 📚 Документация
- Техническая документация
- Пользовательские руководства
- API документация
- Переводы

### 🧪 Тестирование
- Написание тестов
- Ручное тестирование
- Тестирование производительности
- Тестирование безопасности

### 🌐 Локализация
- Переводы интерфейса
- Культурная адаптация
- Тестирование локализации

## 📊 Метрики качества

### Стандарты кода
- **Pylint score**: Минимум 8.0/10
- **Test coverage**: Минимум 80%
- **Security scan**: Без критических уязвимостей
- **Dependencies**: Актуальные версии

### Процесс ревью
- Все изменения проходят ревью
- Автоматические проверки должны проходить
- Документация обновляется при необходимости
- Тесты добавляются для новой функциональности

## 🏆 Программа признания

### Уровни участия
- **Contributor**: Первый принятый PR
- **Regular Contributor**: 5+ принятых PR
- **Core Contributor**: 20+ принятых PR + активное участие
- **Maintainer**: Доверенный участник с правами администратора

### Награды
- Упоминание в CONTRIBUTORS.md
- Специальные значки в профиле
- Приоритетная поддержка
- Участие в принятии решений

## ❓ Вопросы?

Если у вас есть вопросы о участии в разработке:

1. Сначала проверьте этот документ
2. Поищите в существующих issues
3. Создайте новый issue с меткой "question"
4. Свяжитесь с мейнтейнерами напрямую

**Контакты для связи:**
- **Email**: admin@baimuras.space
- **Telegram**: @baimuras_support
- **GitHub**: @ardakchapaev

---

**Спасибо за участие в разработке BaiMuras Platform! 🚀**

*Вместе мы создаем лучшую платформу для творческих профессионалов.*
