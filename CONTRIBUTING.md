
# Руководство для участников проекта Baimuras.Space

Добро пожаловать в проект интеллектуальной экосистемы мебельного инжиниринга! Мы рады, что вы хотите внести свой вклад в развитие проекта.

## 🚀 Быстрый старт

### Настройка среды разработки

1. **Клонирование репозитория**
   ```bash
   git clone https://github.com/ardakchapaev/baimuras.space.git
   cd baimuras.space
   ```

2. **Создание виртуального окружения**
   ```bash
   python -m venv venv
   source venv/bin/activate  # Linux/Mac
   # или
   venv\Scripts\activate     # Windows
   ```

3. **Установка зависимостей**
   ```bash
   pip install -r requirements.txt
   ```

4. **Запуск приложения**
   ```bash
   python src/main.py
   ```

## 📋 Стандарты разработки

### Структура проекта
```
baimuras.space/
├── src/                    # Исходный код приложения
│   ├── main.py            # Точка входа
│   ├── config.py          # Конфигурация
│   ├── errors.py          # Обработка ошибок
│   ├── routes/            # Маршруты Flask
│   └── models/            # Модели данных
├── tests/                 # Тесты (планируется)
├── .github/workflows/     # GitHub Actions
├── requirements.txt       # Python зависимости
├── runtime.txt           # Версия Python
├── Procfile              # Конфигурация деплоя
└── README.md             # Документация
```

### Стиль кодирования

#### Python
- Следуем **PEP 8** стандарту
- Максимальная длина строки: **100 символов**
- Используем **4 пробела** для отступов
- Обязательные **type hints** для новых функций
- Документация в формате **Google Style**

#### Пример хорошего кода:
```python
def calculate_furniture_cost(
    material_type: str,
    dimensions: Dict[str, float],
    quantity: int = 1
) -> float:
    """Рассчитывает стоимость мебели.
    
    Args:
        material_type: Тип материала (дерево, металл, пластик)
        dimensions: Словарь с размерами (длина, ширина, высота)
        quantity: Количество единиц
        
    Returns:
        Общая стоимость в рублях
        
    Raises:
        ValueError: Если переданы некорректные параметры
    """
    if quantity <= 0:
        raise ValueError("Количество должно быть положительным числом")
    
    base_cost = _get_material_base_cost(material_type)
    volume = dimensions['length'] * dimensions['width'] * dimensions['height']
    
    return base_cost * volume * quantity
```

### Проверка качества кода

Перед отправкой кода обязательно запустите:

```bash
# Проверка стиля кода
pylint src/

# Форматирование кода (если установлен black)
black src/

# Проверка типов (если установлен mypy)
mypy src/
```

## 🔄 Процесс разработки

### Создание новой функции

1. **Создайте новую ветку**
   ```bash
   git checkout -b feature/название-функции
   ```

2. **Внесите изменения**
   - Пишите чистый, документированный код
   - Добавляйте тесты для новой функциональности
   - Обновляйте документацию при необходимости

3. **Проверьте код**
   ```bash
   pylint src/
   python -m pytest tests/  # когда тесты будут добавлены
   ```

4. **Зафиксируйте изменения**
   ```bash
   git add .
   git commit -m "feat: добавлена функция расчета стоимости мебели"
   ```

### Правила именования коммитов

Используем [Conventional Commits](https://www.conventionalcommits.org/):

- `feat:` - новая функция
- `fix:` - исправление ошибки
- `docs:` - изменения в документации
- `style:` - форматирование кода
- `refactor:` - рефакторинг кода
- `test:` - добавление тестов
- `chore:` - обновление зависимостей, настройки

**Примеры:**
```bash
git commit -m "feat: добавлен калькулятор стоимости материалов"
git commit -m "fix: исправлена ошибка в расчете объема"
git commit -m "docs: обновлена документация API"
```

### Создание Pull Request

1. **Отправьте ветку на GitHub**
   ```bash
   git push origin feature/название-функции
   ```

2. **Создайте Pull Request**
   - Используйте понятное название
   - Опишите, что изменено и зачем
   - Добавьте скриншоты, если есть изменения в UI
   - Укажите связанные Issues

3. **Шаблон описания PR:**
   ```markdown
   ## Описание
   Краткое описание изменений
   
   ## Тип изменений
   - [ ] Исправление ошибки
   - [ ] Новая функция
   - [ ] Критическое изменение
   - [ ] Обновление документации
   
   ## Тестирование
   - [ ] Код протестирован локально
   - [ ] Добавлены новые тесты
   - [ ] Все существующие тесты проходят
   
   ## Чеклист
   - [ ] Код соответствует стандартам проекта
   - [ ] Добавлена документация
   - [ ] Обновлен CHANGELOG.md (если необходимо)
   ```

## 🧪 Тестирование

### Написание тестов

Используем **pytest** для тестирования:

```python
# tests/test_calculator.py
import pytest
from src.calculator import calculate_furniture_cost

def test_calculate_furniture_cost_basic():
    """Тест базового расчета стоимости."""
    dimensions = {'length': 2.0, 'width': 1.0, 'height': 0.8}
    cost = calculate_furniture_cost('wood', dimensions, 1)
    assert cost > 0

def test_calculate_furniture_cost_invalid_quantity():
    """Тест обработки некорректного количества."""
    dimensions = {'length': 2.0, 'width': 1.0, 'height': 0.8}
    with pytest.raises(ValueError):
        calculate_furniture_cost('wood', dimensions, -1)
```

### Запуск тестов

```bash
# Запуск всех тестов
pytest

# Запуск с покрытием кода
pytest --cov=src

# Запуск конкретного теста
pytest tests/test_calculator.py::test_calculate_furniture_cost_basic
```

## 🐛 Сообщение об ошибках

### Создание Issue

При обнаружении ошибки создайте Issue с:

1. **Четким описанием проблемы**
2. **Шагами для воспроизведения**
3. **Ожидаемым поведением**
4. **Фактическим поведением**
5. **Информацией о среде** (ОС, версия Python, браузер)

### Шаблон Issue:
```markdown
## Описание ошибки
Краткое описание того, что происходит

## Шаги для воспроизведения
1. Перейти на страницу '...'
2. Нажать на '...'
3. Прокрутить вниз до '...'
4. Увидеть ошибку

## Ожидаемое поведение
Что должно было произойти

## Скриншоты
Если применимо, добавьте скриншоты

## Информация о среде:
- ОС: [например, Windows 10]
- Браузер: [например, Chrome 91]
- Версия Python: [например, 3.11]
```

## 📚 Документация

### API Документация

Для документирования API используем docstrings в формате Google:

```python
def create_furniture_order(customer_id: int, items: List[Dict]) -> Dict:
    """Создает заказ на изготовление мебели.
    
    Args:
        customer_id: ID клиента в системе
        items: Список товаров с параметрами:
            - type: тип мебели
            - material: материал
            - dimensions: размеры
            - quantity: количество
    
    Returns:
        Словарь с информацией о созданном заказе:
            - order_id: ID заказа
            - total_cost: общая стоимость
            - estimated_delivery: предполагаемая дата доставки
    
    Raises:
        ValueError: Если переданы некорректные параметры
        CustomerNotFoundError: Если клиент не найден
    
    Example:
        >>> items = [{
        ...     'type': 'table',
        ...     'material': 'oak',
        ...     'dimensions': {'length': 1.5, 'width': 0.8, 'height': 0.75},
        ...     'quantity': 1
        ... }]
        >>> order = create_furniture_order(123, items)
        >>> print(order['order_id'])
        'ORD-2025-001'
    """
```

## 🤝 Сообщество

### Коммуникация

- **GitHub Issues** - для обсуждения ошибок и новых функций
- **GitHub Discussions** - для общих вопросов и идей
- **Pull Requests** - для обзора кода

### Кодекс поведения

- Будьте уважительны к другим участникам
- Конструктивно критикуйте код, а не людей
- Помогайте новичкам разобраться в проекте
- Используйте понятный и профессиональный язык

## 📈 Развитие проекта

### Приоритетные направления

1. **Функциональность**
   - Калькулятор стоимости мебели
   - Система управления заказами
   - Интеграция с производством

2. **Качество**
   - Покрытие тестами > 80%
   - Автоматические проверки качества кода
   - Документация API

3. **Производительность**
   - Оптимизация запросов к базе данных
   - Кэширование часто используемых данных
   - Мониторинг производительности

### Roadmap

- **Q2 2025**: Базовая функциональность калькулятора
- **Q3 2025**: Система управления заказами
- **Q4 2025**: Интеграция с производственными системами
- **Q1 2026**: Мобильное приложение

## ❓ Часто задаваемые вопросы

**Q: Как запустить проект локально?**
A: Следуйте инструкциям в разделе "Быстрый старт"

**Q: Какую версию Python использовать?**
A: Python 3.11 (указано в runtime.txt)

**Q: Где найти документацию API?**
A: Документация генерируется автоматически из docstrings

**Q: Как предложить новую функцию?**
A: Создайте Issue с меткой "enhancement" и подробным описанием

## 📞 Контакты

- **Maintainer**: Ардак Чапаев
- **GitHub**: [@ardakchapaev](https://github.com/ardakchapaev)
- **Email**: [указать email если необходимо]

---

Спасибо за ваш вклад в развитие проекта Baimuras.Space! 🚀

*Последнее обновление: 17 июня 2025 г.*
