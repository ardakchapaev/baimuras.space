# 🔧 Конфигурация BaiMuras Platform
# Скопируйте этот файл в .env и заполните своими значениями

# =============================================================================
# ОСНОВНЫЕ НАСТРОЙКИ
# =============================================================================

# Режим работы приложения (development, production, testing)
FLASK_ENV=development

# Секретный ключ для Flask (генерируйте уникальный для продакшн)
SECRET_KEY=your-super-secret-key-change-this-in-production

# Отладочный режим (True только для разработки)
FLASK_DEBUG=True

# Хост и порт для запуска приложения
FLASK_HOST=127.0.0.1
FLASK_PORT=5000

# =============================================================================
# БАЗА ДАННЫХ
# =============================================================================

# URL подключения к PostgreSQL
DATABASE_URL=postgresql://username:password@localhost:5432/baimuras_db

# Настройки пула соединений
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=3600

# =============================================================================
# REDIS (КЕШИРОВАНИЕ И СЕССИИ)
# =============================================================================

# URL подключения к Redis
REDIS_URL=redis://localhost:6379/0

# Настройки кеширования
CACHE_TYPE=redis
CACHE_DEFAULT_TIMEOUT=300
CACHE_KEY_PREFIX=baimuras_

# =============================================================================
# ПОЧТОВЫЕ НАСТРОЙКИ
# =============================================================================

# SMTP сервер
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USE_SSL=False

# Учетные данные почты
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password

# Отправитель по умолчанию
MAIL_DEFAULT_SENDER=noreply@baimuras.space

# Администраторы (получают уведомления об ошибках)
ADMINS=admin@baimuras.space,support@baimuras.space

# =============================================================================
# ФАЙЛЫ И ЗАГРУЗКИ
# =============================================================================

# Папка для загрузки файлов
UPLOAD_FOLDER=/path/to/uploads

# Максимальный размер файла (в байтах, 16MB)
MAX_CONTENT_LENGTH=16777216

# Разрешенные расширения файлов
ALLOWED_EXTENSIONS=png,jpg,jpeg,gif,pdf,doc,docx,txt

# =============================================================================
# БЕЗОПАСНОСТЬ
# =============================================================================

# Настройки CSRF защиты
WTF_CSRF_ENABLED=True
WTF_CSRF_TIME_LIMIT=3600

# Настройки сессий
SESSION_COOKIE_SECURE=False  # True для HTTPS
SESSION_COOKIE_HTTPONLY=True
SESSION_COOKIE_SAMESITE=Lax
PERMANENT_SESSION_LIFETIME=7200  # 2 часа в секундах

# Настройки безопасности заголовков
SECURITY_HEADERS_ENABLED=True

# =============================================================================
# API НАСТРОЙКИ
# =============================================================================

# Версия API
API_VERSION=v1

# Rate limiting (запросов в час)
API_RATE_LIMIT_AUTHENTICATED=1000
API_RATE_LIMIT_ANONYMOUS=100
API_RATE_LIMIT_ADMIN=5000

# JWT настройки
JWT_SECRET_KEY=your-jwt-secret-key
JWT_ACCESS_TOKEN_EXPIRES=3600  # 1 час
JWT_REFRESH_TOKEN_EXPIRES=2592000  # 30 дней

# =============================================================================
# ВНЕШНИЕ СЕРВИСЫ
# =============================================================================

# Google Analytics
GOOGLE_ANALYTICS_ID=GA_MEASUREMENT_ID

# Google reCAPTCHA
RECAPTCHA_SITE_KEY=your-recaptcha-site-key
RECAPTCHA_SECRET_KEY=your-recaptcha-secret-key

# Cloudflare (если используется)
CLOUDFLARE_API_TOKEN=your-cloudflare-token
CLOUDFLARE_ZONE_ID=your-zone-id

# =============================================================================
# СОЦИАЛЬНЫЕ СЕТИ (OAuth)
# =============================================================================

# Google OAuth
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Facebook OAuth
FACEBOOK_APP_ID=your-facebook-app-id
FACEBOOK_APP_SECRET=your-facebook-app-secret

# GitHub OAuth
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# =============================================================================
# МОНИТОРИНГ И ЛОГИРОВАНИЕ
# =============================================================================

# Уровень логирования (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Папка для логов
LOG_FOLDER=/var/log/baimuras

# Sentry для отслеживания ошибок (опционально)
SENTRY_DSN=your-sentry-dsn

# =============================================================================
# CELERY (ФОНОВЫЕ ЗАДАЧИ)
# =============================================================================

# Брокер для Celery (Redis или RabbitMQ)
CELERY_BROKER_URL=redis://localhost:6379/1
CELERY_RESULT_BACKEND=redis://localhost:6379/2

# =============================================================================
# ELASTICSEARCH (ПОИСК)
# =============================================================================

# URL Elasticsearch (опционально)
ELASTICSEARCH_URL=http://localhost:9200

# =============================================================================
# WEBHOOK НАСТРОЙКИ
# =============================================================================

# Секретный ключ для webhook подписей
WEBHOOK_SECRET=your-webhook-secret

# =============================================================================
# РАЗРАБОТКА И ТЕСТИРОВАНИЕ
# =============================================================================

# Настройки для тестирования
TESTING=False
TEST_DATABASE_URL=postgresql://username:password@localhost:5432/baimuras_test

# Профилирование производительности
PROFILING_ENABLED=False

# =============================================================================
# ПРОДАКШН НАСТРОЙКИ
# =============================================================================

# Домен приложения
SERVER_NAME=baimuras.space

# SSL настройки
PREFERRED_URL_SCHEME=https

# Gunicorn настройки
WORKERS=4
WORKER_CLASS=gevent
WORKER_CONNECTIONS=1000
MAX_REQUESTS=1000
MAX_REQUESTS_JITTER=100
TIMEOUT=30
KEEPALIVE=2

# =============================================================================
# ДОПОЛНИТЕЛЬНЫЕ НАСТРОЙКИ
# =============================================================================

# Часовой пояс
TIMEZONE=Asia/Almaty

# Язык по умолчанию
DEFAULT_LANGUAGE=ru

# Поддерживаемые языки
LANGUAGES=ru,kz,en

# Валюта по умолчанию
DEFAULT_CURRENCY=KZT

# =============================================================================
# ПРИМЕЧАНИЯ
# =============================================================================

# 1. Никогда не коммитьте файл .env в репозиторий
# 2. Используйте сильные пароли и ключи в продакшн
# 3. Регулярно обновляйте секретные ключи
# 4. Для продакшн используйте переменные окружения вместо .env файла
# 5. Проверьте все настройки перед развертыванием

# =============================================================================
# ГЕНЕРАЦИЯ СЕКРЕТНЫХ КЛЮЧЕЙ
# =============================================================================

# Для генерации секретного ключа используйте:
# python -c "import secrets; print(secrets.token_urlsafe(32))"

# Для генерации UUID:
# python -c "import uuid; print(str(uuid.uuid4()))"
